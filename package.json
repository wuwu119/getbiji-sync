{
  "name": "getbiji-sync",
  "version": "0.1.0",
  "description": "Obsidian plugin to sync Get笔记 (getbiji.com) notes",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run",
    "test:watch": "vitest",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [],
  "author": "wuwu",
  "license": "MIT",
  "devDependencies": {
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.17.3",
    "obsidian": "latest",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
